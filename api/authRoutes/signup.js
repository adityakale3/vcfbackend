const express = require('express');
const router = express.Router();

// Signup User
router.get('/signup', (req,res) => {
    // Get Data from post
    // var username = req.params.username;

    // Save User

    // Fetch Response

    // Send Response

    res.send(`Signup API | Protected Route`);
});


module.exports = router;