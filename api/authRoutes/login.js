const express = require('express');
const router = express.Router();

// Login User
router.get('/login', (req,res) => {
    // Get Data from post
    // var username = req.params.username;

    // Validate User

    // Fetch Response

    // Send Response

    res.send(`Login API | Protected Route`);
});


module.exports = router;